master:
  hosts:
    k3s-master-a:
  vars:
    k3s_control_node: true
    k3s_tls_san: "{{ hostvars[groups['master'][0]]['ansible_host'] }}"

nodes:
  hosts:
    k8s-01:
    k8s-02:
    k8s-03:
  vars:

ceph:
  hosts:
    k8s-01:
    k8s-02:
    k8s-03:
  vars:

nfs:
  hosts:
  vars:

k3s_cluster:
  children:
    master:
    nodes:

storage:
  children:
    ceph:
    nfs:

vault:
  hosts:
    localhost:
      ansible_connection: local
